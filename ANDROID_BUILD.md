# Auction Retro Store — Android build

Branch: `auction-retro-android`

The source is reconstructed by `android/prepare_source.py` and built by GitHub Actions as an arm64 debug APK.

Workflow artifact name: `AuctionRetroStore-Android-v1.0.2-arm64`.
